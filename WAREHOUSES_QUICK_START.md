# دليل البدء السريع - ميزة المخازن المتعددة

## 🎯 ما تم إضافته؟

تم إضافة **نظام إدارة المخازن المتعددة** الذي يتيح لك:
- ✅ إضافة وإدارة عدة مخازن
- ✅ التبديل السريع بين المخازن من الشريط الجانبي
- ✅ عرض إحصائيات مفصلة لكل مخزن
- ✅ واجهة احترافية وجميلة بتصميم عصري

## 🚀 كيف تستخدم الميزة؟

### 1️⃣ الوصول إلى صفحة المخازن
- انتقل إلى **المخازن** من القائمة الجانبية
- أو اذهب مباشرة إلى: `/warehouses`

### 2️⃣ إضافة مخزن جديد
1. اضغط على زر **"إضافة مخزن"**
2. املأ البيانات:
   - اسم المخزن
   - الموقع
   - المدير المسؤول
   - رقم الهاتف
   - البريد الإلكتروني
   - الوصف
3. اضغط **"حفظ"**

### 3️⃣ التبديل بين المخازن
- افتح القائمة المنسدلة في أعلى الشريط الجانبي
- اختر المخزن الذي تريد العمل فيه
- سيتم حفظ اختيارك تلقائيًا

### 4️⃣ تعديل مخزن
1. اضغط على أيقونة **⋮** (ثلاث نقاط) على بطاقة المخزن
2. اختر **"تعديل"**
3. عدّل البيانات
4. اضغط **"حفظ"**

### 5️⃣ حذف مخزن
1. اضغط على أيقونة **⋮** على بطاقة المخزن
2. اختر **"حذف"**
3. أكّد الحذف

## 📊 الإحصائيات المعروضة

### في صفحة المخازن:
- **إجمالي المخازن**: عدد المخازن الكلي والمخازن النشطة
- **إجمالي المنتجات**: مجموع المنتجات في كل المخازن
- **قيمة المخزون الإجمالية**: القيمة المالية لجميع المنتجات
- **تنبيهات المخزون المنخفض**: عدد المنتجات التي تحتاج إعادة تعبئة

### لكل مخزن:
- عدد المنتجات
- القيمة الإجمالية
- عدد المنتجات ذات المخزون المنخفض
- معلومات الاتصال (الموقع، المدير، الهاتف، البريد)

## 🎨 التصميم والميزات الجمالية

### البطاقات الاحترافية:
- ✨ تدرج لوني في أعلى كل بطاقة
- 🎯 أيقونة بتدرج لوني جميل
- 🖱️ تأثيرات hover تفاعلية
- 📱 تصميم متجاوب لجميع الشاشات

### الألوان المميزة:
- 🔵 أزرق للمخازن
- 🟢 أخضر للمنتجات
- 🟣 بنفسجي للقيم المالية
- 🟠 برتقالي للتنبيهات

## 💾 حفظ البيانات

البيانات يتم حفظها في:
- **localStorage** للمتصفح (بيانات محلية)
- يتم حفظ:
  - قائمة جميع المخازن
  - المخزن المختار حاليًا
- البيانات تبقى حتى عند إغلاق المتصفح وإعادة فتحه

## 🔧 للمطورين

### استخدام Context في أي صفحة:

```tsx
import { useWarehouse } from "@/contexts/warehouse-context";
import { useCurrentWarehouse } from "@/hooks/use-current-warehouse";

function MyPage() {
  // الحصول على المخزن الحالي
  const currentWarehouse = useCurrentWarehouse();
  
  // الحصول على جميع الوظائف
  const { 
    warehouses, 
    addWarehouse, 
    updateWarehouse, 
    deleteWarehouse 
  } = useWarehouse();
  
  return (
    <div>
      <p>المخزن الحالي: {currentWarehouse?.name}</p>
    </div>
  );
}
```

### استخدام الإحصائيات:

```tsx
import { useWarehouseStats } from "@/hooks/use-current-warehouse";

function StatsComponent() {
  const stats = useWarehouseStats();
  
  return (
    <div>
      <p>إجمالي المخازن: {stats.totalWarehouses}</p>
      <p>إجمالي المنتجات: {stats.totalProducts}</p>
      <p>القيمة الإجمالية: {stats.totalValue}</p>
    </div>
  );
}
```

## 📝 بيانات تجريبية

تم إضافة 3 مخازن تجريبية:
1. **المخزن الرئيسي** (الرياض)
2. **مخزن الفرع الشمالي** (جدة)
3. **مخزن الإلكترونيات** (الدمام)

يمكنك حذفها وإضافة مخازنك الخاصة.

## 🌍 دعم اللغات

الميزة تدعم بالكامل:
- ✅ **العربية** (ar) - مع دعم RTL
- ✅ **الإنجليزية** (en) - مع دعم LTR

التبديل بين اللغات يتم من الإعدادات.

## ⚡ نصائح الاستخدام

1. **اختر مخزنًا دائمًا**: قبل العمل على المنتجات أو الطلبات
2. **راقب التنبيهات**: تحقق من المنتجات ذات المخزون المنخفض
3. **حدّث البيانات**: عدّل معلومات المخازن عند التغيير
4. **استخدم الوصف**: اكتب وصفًا واضحًا لكل مخزن لسهولة التمييز

## 🔮 التطويرات القادمة (اختياري)

يمكنك إضافة:
- نقل المنتجات بين المخازن
- تقارير مقارنة الأداء
- تنبيهات تلقائية للمخزون المنخفض
- خريطة لمواقع المخازن
- صلاحيات وأذونات لكل مخزن
- سجل تاريخ العمليات لكل مخزن

## 📞 هل تحتاج مساعدة؟

راجع الملفات التالية:
- `WAREHOUSES_FEATURE.md` - شرح تفصيلي كامل
- `WAREHOUSE_INTEGRATION_EXAMPLE.md` - أمثلة التكامل مع الصفحات الأخرى

---

**تم التطوير بواسطة**: Kiro AI  
**التاريخ**: 2024  
**الإصدار**: 1.0.0
