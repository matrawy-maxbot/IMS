# ميزة إدارة المخازن المتعددة

## نظرة عامة
تم إضافة ميزة إدارة المخازن المتعددة إلى نظام إدارة المخزون (IMS). هذه الميزة تتيح للمستخدمين إدارة عدة مخازن، والتبديل بينها بسهولة، وعرض إحصائيات مفصلة لكل مخزن.

## الملفات المضافة/المعدلة

### ملفات جديدة:
1. **`src/app/warehouses/page.tsx`** - صفحة إدارة المخازن الرئيسية
2. **`src/contexts/warehouse-context.tsx`** - Context لإدارة حالة المخازن
3. **`src/i18n/messages/ar/warehouses.json`** - ترجمات عربية
4. **`src/i18n/messages/en/warehouses.json`** - ترجمات إنجليزية

### ملفات معدلة:
1. **`src/components/sidebar.tsx`** - إضافة Select Box للمخازن
2. **`src/app/layout.tsx`** - إضافة WarehouseProvider
3. **`src/i18n/messages/ar/nav.json`** - إضافة ترجمات التنقل
4. **`src/i18n/messages/en/nav.json`** - إضافة ترجمات التنقل
5. **`src/app/globals.css`** - إضافة أنماط للاتجاه

## المميزات

### 1. صفحة إدارة المخازن (`/warehouses`)
- **عرض شبكي جميل**: عرض المخازن في شكل بطاقات (Cards) احترافية ومتناسقة
- **إحصائيات شاملة**: 
  - إجمالي المخازن
  - إجمالي المنتجات في جميع المخازن
  - قيمة المخزون الكلية
  - تنبيهات المخزون المنخفض
- **لكل مخزن**:
  - معلومات الاتصال (الموقع، المدير، الهاتف، البريد)
  - إحصائيات مفصلة (عدد المنتجات، القيمة، المنتجات المنخفضة)
  - حالة المخزن (نشط/غير نشط)

### 2. التصميم الإبداعي
- **تدرجات لونية**: كل بطاقة مخزن لها تدرج لوني جميل في الأعلى
- **أيقونات ملونة**: أيقونات بألوان مميزة لكل نوع من المعلومات
- **تأثيرات تفاعلية**: 
  - تأثير hover على البطاقات
  - ظلال ديناميكية
  - انتقالات سلسة
- **بطاقات إحصائيات**: كل بطاقة إحصائية لها حدود ملونة مميزة

### 3. إدارة المخازن
- **إضافة مخزن جديد**: نموذج شامل لإضافة مخزن بكل تفاصيله
- **تعديل المخزن**: تحديث معلومات أي مخزن بسهولة
- **حذف المخزن**: حذف المخازن مع تأكيد الإجراء
- **قائمة الإجراءات**: قائمة منسدلة لكل مخزن للوصول السريع للإجراءات

### 4. Select Box في Sidebar
- **اختيار المخزن الحالي**: Select Box احترافي في الشريط الجانبي
- **التبديل السريع**: انتقال فوري بين المخازن
- **رابط سريع**: رابط "إدارة المخازن" داخل القائمة المنسدلة
- **حفظ الاختيار**: يتم حفظ المخزن المختار في localStorage

### 5. Context API
- **إدارة الحالة المركزية**: جميع البيانات محفوظة في Context
- **مشاركة البيانات**: إمكانية الوصول للمخزن الحالي من أي مكان في التطبيق
- **LocalStorage**: حفظ واستعادة البيانات تلقائيًا

## الاستخدام

### الوصول إلى صفحة المخازن
```typescript
// من خلال الشريط الجانبي
// انقر على "المخازن" في القائمة

// أو مباشرة
// http://localhost:3000/warehouses
```

### استخدام Context في المكونات
```typescript
import { useWarehouse } from '@/contexts/warehouse-context';

function MyComponent() {
  const { 
    warehouses,           // جميع المخازن
    selectedWarehouse,    // المخزن المختار حاليًا
    setSelectedWarehouse, // تغيير المخزن
    addWarehouse,        // إضافة مخزن
    updateWarehouse,     // تحديث مخزن
    deleteWarehouse      // حذف مخزن
  } = useWarehouse();

  // استخدم المخزن الحالي
  console.log(selectedWarehouse?.name);
}
```

## التصميم

### الألوان المستخدمة
- **أزرق**: للمخازن والإحصائيات الرئيسية
- **أخضر**: للمنتجات والإحصائيات الإيجابية
- **بنفسجي**: للقيم المالية
- **برتقالي**: للتنبيهات والمخزون المنخفض

### التدرجات اللونية
```css
/* تدرج أعلى البطاقة */
background: linear-gradient(to right, #3b82f6, #a855f7, #ec4899);

/* أيقونة المخزن */
background: linear-gradient(to bottom right, #3b82f6, #9333ea);
```

## البيانات المحفوظة

### Structure
```typescript
interface Warehouse {
  id: string;
  name: string;
  location: string;
  manager: string;
  phone: string;
  email: string;
  description: string;
  totalProducts: number;
  totalValue: number;
  lowStockItems: number;
  status: "active" | "inactive";
  createdAt: string;
}
```

### LocalStorage Keys
- `warehouses`: قائمة جميع المخازن
- `selectedWarehouseId`: معرف المخزن المختار حاليًا

## التطويرات المستقبلية المقترحة

1. **ربط مع قاعدة البيانات**: حاليًا البيانات محلية، يمكن ربطها مع API
2. **صفحة تفاصيل المخزن**: صفحة مخصصة لكل مخزن مع إحصائيات مفصلة
3. **تقارير المخازن**: تقارير مقارنة بين المخازن
4. **نقل المنتجات**: إمكانية نقل المنتجات بين المخازن
5. **أذونات المخازن**: تحديد صلاحيات للمستخدمين لكل مخزن
6. **إشعارات**: إشعارات عند انخفاض المخزون في أي مخزن
7. **خريطة المواقع**: عرض مواقع المخازن على خريطة تفاعلية

## دعم اللغات

الميزة تدعم بالكامل:
- ✅ العربية (ar)
- ✅ الإنجليزية (en)

جميع النصوص قابلة للترجمة باستخدام next-intl.

## الأداء

- ✅ تحميل سريع مع React Context
- ✅ حفظ تلقائي في localStorage
- ✅ تحديثات فورية للواجهة
- ✅ تفاعلات سلسة مع الرسوم المتحركة

## التوافق

- ✅ دعم RTL/LTR كامل
- ✅ دعم الوضع الليلي/النهاري
- ✅ متجاوب مع جميع أحجام الشاشات
- ✅ يعمل على جميع المتصفحات الحديثة
